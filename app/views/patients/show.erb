<h2>Patient's show page:</h2>
<h3>Patient name: <%= @patient.name %></h3>
<h4>Appointments: </h4>
<ul>
  <% @appointments.each do |app| %>
  <li>Doctor: <%= link_to app.doctor.name, doctor_path(app.doctor) %></li>
  <li>Appointment: <%= app.date %> at <%= app.time %></li>
  <br>
  <% end %>
</ul>

<!-- Patient#show page that lists the date and time for
each of their appointments and links to the corresponding
 doctor's show page. -->
