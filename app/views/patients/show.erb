<h1><%= @patient.name %></h1>
<h2><%= pluralize(@patient.age, "year") %> old</h2>
<br>
<h3>Appointments:</h3>
<ul>
  <% @appointments.each do |app| %>
    <li><%= link_to app.doctor.name, doctor_path(app.doctor) %>: <%= app.datetime %></li>
  <% end %>
</ul>
