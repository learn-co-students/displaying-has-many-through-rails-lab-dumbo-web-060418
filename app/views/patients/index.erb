<h2>All Patients:</h2>
<ul>
  <% @patients.each do |patient| %>
    <li><b>Name: <%= link_to patient.name, patient_path(patient) %></b></li>
      <li>Number of Appointments: <%= patient.app_count %></li>
      <br>
  <% end %>
</ul>
