<h2>Doctor show page</h2>
<h2>Doctor's Name: <%= @doctor.name %></h2>
<h2>Department: <%= @doctor.department %></h2>
<h3>Patients:</h3>
<ul>
<% @appointments.each do |app| %>
  <li><b>Patient name: <%= link_to app.patient.name, patient_path(app.patient) %></b></li>
  <li>Appointment: <%= app.date %> at <%= app.time %></li>
  <br>
  <% end %>
</ul>

<!-- page that displays the doctor's name, department,
and appointments, with each appointment's date,
time, and patient name (linking to the patient's
show page). -->
